# Pinned for reproducibility. Everything here is offline at runtime.
#
# These versions MUST match the environment `tools/train_adjudicator.py` runs
# in. A model pickled under one scikit-learn version and unpickled under another
# loads with a warning rather than an error and still produces plausible output,
# so the mismatch is silent. `tools/check_env.py` asserts the match; run it
# before building the image.
PyMuPDF==1.28.0
pytesseract==0.3.13
Pillow==12.3.0
numpy==2.5.1
# Learned adjudicator (EVALUATION.md permits candidate-trained models offline).
scikit-learn==1.9.0
joblib==1.5.3
# Transitive, pinned anyway: an unpinned scipy is free to move under a pinned
# scikit-learn and reintroduce the same silent mismatch.
scipy==1.18.0
threadpoolctl==3.6.0
# Independent OCR fallback for unresolved raster fields.
rapidocr-onnxruntime==1.4.4
onnxruntime==1.28.0
opencv-python==5.0.0.93
pyclipper==1.4.0
Shapely==2.1.2
PyYAML==6.0.3
six==1.17.0
tqdm==4.70.0
